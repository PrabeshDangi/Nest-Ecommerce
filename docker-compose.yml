# services:
#   nest-backend:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     env_file:
#       - .env
#     volumes:
#       - .:/app
#     ports:
#       - '3000:3000'

services:
  nest-app:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - app-network

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - '80:80'
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
